var MixerLib = function(options, debug) {
        if (!(this instanceof MixerLib)) { return new MixerLib(options, debug); }
        return this; // unnecessary, but it makes validators happy
    }
;

exports.MixerLib = MixerLib;
